export const Separator = (props) => {
    return (
        <>
        <div className="separator"> 
            <div className="section-header">
            {props.header}
            </div>
        </div>
    </>
    );
}